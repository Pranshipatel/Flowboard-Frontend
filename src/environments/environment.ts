export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/api/v1', // API Gateway
  razorpayKey: '', // Backend sends the real key from environment-backed server config.
};
